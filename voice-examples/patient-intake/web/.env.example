# Credentials for the LiveKit Cloud project hosting the agent worker.
# The browser never sees these — they are used server-side to mint a short-lived
# participant token in app/api/agent/connection_details/route.ts.
LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
